#include<bits/stdc++.h>

using namespace std;

int main(){

    vector<int> v;

    v.push_back(19);
    v.push_back(20);
    v.push_back(30);
    //v.size()  => py len()
    for (int i = 0; i < v.size(); i++)
    {
        cout << v[i] << "\n";
    }

    int n, q;
    vector<int> v[n];




    
    return 0;
}
